# MovieFastAPI

REST API для управления базой данных фильмов, построенный на FastAPI.

## 📋 Описание

MovieFastAPI - это современный веб-сервис для работы с информацией о фильмах. Проект предоставляет RESTful API для создания, чтения, обновления и удаления данных о фильмах, с возможностью фильтрации и поиска.

## ✨ Возможности

- 🎬 CRUD операции для фильмов
- 🔍 Поиск и фильтрация фильмов
- 📊 Автоматическая интерактивная документация API (Swagger UI)
- ✅ Валидация данных с помощью Pydantic
- 🗄️ Работа с базой данных через SQLAlchemy
- ⚡ Асинхронная обработка запросов

## 🛠️ Технологии

- **Python 3.8+**
- **FastAPI** - современный веб-фреймворк
- **SQLAlchemy** - ORM для работы с базой данных
- **Pydantic** - валидация данных
- **Uvicorn** - ASGI сервер
- **PostgreSQL/SQLite** - база данных

## 📦 Установка

### Клонирование репозитория

```bash
git clone https://github.com/SalahidinAI/MovieFastAPI.git
cd MovieFastAPI
```

### Создание виртуального окружения

```bash
python -m venv venv
source venv/bin/activate  # для Linux/Mac
# или
venv\Scripts\activate  # для Windows
```

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Настройка базы данных

Создайте файл `.env` в корневой директории проекта:

```env
DATABASE_URL=postgresql://user:password@localhost:5432/moviedb
# или для SQLite
# DATABASE_URL=sqlite:///./movies.db

SECRET_KEY=your-secret-key-here
```

### Инициализация базы данных

```bash
# Выполните миграции
alembic upgrade head
```

## 🚀 Запуск

### Режим разработки

```bash
uvicorn main:app --reload
```

### Режим продакшена

```bash
uvicorn main:app --host 0.0.0.0 --port 8000
```

После запуска API будет доступен по адресу: `http://localhost:8000`

## 📚 Документация API

После запуска приложения, интерактивная документация доступна по адресам:

- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

## 🔌 Основные эндпоинты

### Фильмы

```http
GET    /api/movies          # Получить список всех фильмов
GET    /api/movies/{id}     # Получить фильм по ID
POST   /api/movies          # Создать новый фильм
PUT    /api/movies/{id}     # Обновить фильм
DELETE /api/movies/{id}     # Удалить фильм
```

### Примеры запросов

#### Получение всех фильмов

```bash
curl -X GET "http://localhost:8000/api/movies"
```

#### Создание нового фильма

```bash
curl -X POST "http://localhost:8000/api/movies" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Inception",
    "director": "Christopher Nolan",
    "year": 2010,
    "genre": "Sci-Fi",
    "rating": 8.8
  }'
```

#### Получение фильма по ID

```bash
curl -X GET "http://localhost:8000/api/movies/1"
```

## 📝 Структура проекта

```
MovieFastAPI/
├── app/
│   ├── __init__.py
│   ├── main.py              # Точка входа приложения
│   ├── models/              # Модели базы данных
│   │   └── movie.py
│   ├── schemas/             # Pydantic схемы
│   │   └── movie.py
│   ├── routers/             # API роутеры
│   │   └── movies.py
│   ├── database.py          # Конфигурация БД
│   └── config.py            # Настройки приложения
├── alembic/                 # Миграции базы данных
├── tests/                   # Тесты
├── .env                     # Переменные окружения
├── .gitignore
├── requirements.txt         # Зависимости
└── README.md
```

## 🧪 Тестирование

Запуск тестов:

```bash
pytest
```

С отображением покрытия кода:

```bash
pytest --cov=app tests/
```

## 🤝 Вклад в проект

Буду рад вашему вкладу в развитие проекта! Если вы хотите внести изменения:

1. Форкните репозиторий
2. Создайте ветку для вашей функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Подробности в файле [LICENSE](LICENSE).

## 👤 Автор

**SalahidinAI**

- GitHub: [@SalahidinAI](https://github.com/SalahidinAI)

## 📞 Контакты

Если у вас есть вопросы или предложения, создайте Issue в репозитории.

---

⭐ Если проект был полезен, поставьте звезду!
